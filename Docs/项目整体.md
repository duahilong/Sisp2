# 项目名称 ：Sisp - System Installation Script Project

## 整体介绍
结合编写的工具、脚本和外部软件，来实现对多个或者单个硬盘的系统安装。通过对硬盘的初始化、格式化、分区、克隆操作系统等一系列操作，实现对硬盘的完整制作。


## 项目技术架构
项目整体所以使用Python编写，结合外部软件和Windows上的脚本，实现完整流程。
- 工具：ghost64.exe、bcdboot.exe 等，更详细的说明 [引用外部软件信息.md](引用外部软件信息.md)
- 脚本：硬盘分区脚本通过Windows上的 powershell 编写。
- 模块：通过python编写模块，来实现各个功能。

## 运行前置条件
- 项目运行环境为 Windows。
- 项目依赖 PowerShell 执行磁盘相关命令。
- 程序需以管理员权限运行。
- 若未以管理员权限运行，程序应在流程开始前终止，并提示用户重新以管理员身份启动。

## Python 项目依赖
- 当前项目使用 `requirements.txt` 管理 Python 依赖。
- 当前已声明依赖：`wcwidth>=0.2.13`
- 初次运行项目前，建议先执行：`py -m pip install -r requirements.txt`

## 运行方式与 JSON 配置文件
- 主程序默认读取 `json/win11.json` 配置文件。
- 可直接执行默认方式运行：`py .\app\main.py`
- 若需要指定其他 JSON 配置文件，可通过 `-j` 参数传入路径。
- 示例：`py .\app\main.py -j .\json\win11.json`
- 示例：`py .\app\main.py -j D:\Code-Project\Sisp2\json\win11.json`

## 项目文件夹目录树

```
Sisp2/
├── .github/                # CI/CD 工作流配置（例如 GitHub Actions，可选）、
├── app/                    # 源代码根目录
├── docs/                   # 项目文档
├── scripts/                # 构建的脚本
├── img/                    # 系统镜像目录和其他大文件目录
├── json/                   # JSON 配置文件目录
├── tests/                  # 测试代码和测试脚本目录
├── sw/                     # 外部软件目录
├── .gitignore              # Git 忽略文件配置（如忽略 __pycache__、.env 等）
├── requirements.txt        # Python 项目依赖列表
└── README.md               # 项目介绍、安装步骤、快速上手指南
```

## 整体运行流程

``` 
运行软件（支持使用 -j 参数传入 JSON 配置文件路径）
检查运行环境和管理员权限
读取json配置文件
获取硬盘信息
终端显示硬盘信息
用户选择硬盘（根据硬盘编号选择单个或多个硬盘）
初始化硬盘
验证初始化结果
硬盘分区和格式化
验证分区和格式化结果
调用外部软件ghost64.exe写入镜像
验证镜像写入结果
拷贝目录到特定位置
验证拷贝结果
调用外部软件bcdboot.exe创建引导记录
验证引导记录创建结果
验证整体完成并返回结果（通过/失败）
```
